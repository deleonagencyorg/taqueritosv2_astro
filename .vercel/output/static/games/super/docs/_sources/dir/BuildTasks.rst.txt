
Build Tasks
============

The following build tasks are available for development and production. In the terminal, type :code:`npx vite [taskname]` to execute the task

* **default**:
    Launch a development server
* **build** 
    Create distrubitions, including minified CSS, Javascript files and all game assets
