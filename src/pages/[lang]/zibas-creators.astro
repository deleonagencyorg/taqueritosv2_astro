---
// Página deshabilitada intencionalmente.
// Esta ruta dinámica se mantiene solo para compatibilidad, pero no genera páginas.

export async function getStaticPaths() {
  // No generamos ninguna página para esta ruta
  return [];
}
---

