---
import { Image } from 'astro:assets';

interface PlayersItem {
    title: string;
    subtitle: string;
    user: string;
    comment: string;
}

interface PlayersHome {
    title: string;
    subtitle: string;
    button: {
        title: string;
    };
}

interface Props {
  players: PlayersItem[];
  playersHome: PlayersHome;
}

const {
  players = [],
  playersHome = { title: '', subtitle: '', button: { title: '' } }
} = Astro.props;
---

{players.length === 0 ? (
  <div class="w-full py-10 flex items-center justify-center bg-[#081806]">
    <p class="text-white text-center text-lg md:text-xl font-semibold">No hay contenido disponible</p>
  </div>
) : (
<div class="players-carousel-container relative overflow-hidden">
  <!-- Header fijo FUERA del carrusel -->
  <div class="flex flex-col md:flex-col lg:flex-row lg:justify-between lg:items-center lg:ml-[20px] lg:mr-[80px] mt-[40px] mb-[40px]">
    {playersHome.title && (
      <div class="container lg:mx-auto lg:px-4 px-6 title">
        <p class="mb-4">
          {playersHome.title}
        </p>
        {playersHome.subtitle && (
          <p>
            {playersHome.subtitle}
          </p>
        )}
      </div>
    )}
    
    <div class="mt-8 md:mt-0 lg:mt-0">
      <button class="gradient-border-button">
        {playersHome.button?.title || "Sé parte de nuestra comunidad"}
      </button>
    </div>
  </div>

  <!-- Carrusel de Swiper -->
  <div class="swiper players-swiper pb-16 ">
    <div class="swiper-wrapper">
      {players.map((player) => {
        return (
          <div class="swiper-slide">
            <div class="slide-content flex items-center justify-center">
              <div class="player-card">
                <h3 class="card-title">
                  {player.title} <br/>
                  {player.subtitle}
                </h3>
                
                <p class="card-user">
                  {player.user}
                </p>
                
                <p class="card-comment">
                  {player.comment}
                </p>
              </div>
            </div>
          </div>
        );
      })}
    </div>
  </div>
</div>
)}

<style>
  .players-carousel-container {
    position: relative;
    width: 100%;
    overflow: hidden;
    background: #081806;
    padding-bottom: 40px;
  }
  
  /* ======= FONDO ======= */
  .players-carousel-container::before,
  .players-carousel-container::after {
    content: "";
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    width: 50vw;
    height: 38vw;
    border-radius: 50%;
    background: linear-gradient(322.3deg, #07C650 7.42%, #00FFAE 82.2%);
    filter: blur(80px);
    opacity: 0.2;
    z-index: 1;
    pointer-events: none;
  }
  
  .players-carousel-container::before { 
    left: -30vw; 
  }
  
  .players-carousel-container::after { 
    right: -30vw;
  }
  
  .slide-content {
    position: relative;
    z-index: 2;
    padding: 20px 0;
  }

  /* ========== AQUÍ: SOLUCIÓN CORRECTA - RTL para pegar a la derecha → ========== */
  @media (min-width: 1024px) {
    .players-swiper {
      direction: rtl; /* Invierte todo el carrusel */
      overflow: visible !important;
    }
    
    /* Corrige la dirección del texto dentro de las cards */
    .players-swiper .swiper-slide {
      direction: ltr;
    }
  }
  /* ============================================================== */
  
  /* ======= CARD ======= */
  .player-card {
    position: relative;
    width: 532px;
    max-width: 90%;
    min-height: 275px;
    padding: 40px 30px;
    background: #092514A6;
    border: none;
    border-radius: 5px;
    z-index: 2;
    backdrop-filter: blur(10px);
  }
  
  /* Borde degradado */
  .player-card::before {
    content: '';
    position: absolute;
    inset: 0;
    border-radius: 5px;
    padding: 1px;
    background: linear-gradient(237.56deg, #2DFF45 1.39%, #9BFF9B 47.74%, #2DFF3B 100%);
    -webkit-mask: 
      linear-gradient(#fff 0 0) content-box, 
      linear-gradient(#fff 0 0);
    -webkit-mask-composite: xor;
    mask-composite: exclude;
    z-index: -1;
  }
  
  .card-title {
    font-size: 24px;
    font-weight: 700;
    color: white;
    text-transform: uppercase;
    font-family: 'Avory I PE', sans-serif;
    line-height: 1.3;
    margin-bottom: 24px;
  }
  
  .card-user {
    font-size: 20px;
    font-weight: 600;
    margin-bottom: 16px;
    color: white;
    font-family: 'Avory I PE', sans-serif;
  }
  
  .card-comment {
    font-size: 20px;
    color: white;
    line-height: 1.6;
    opacity: 0.9;
    font-family: 'Avory I PE', sans-serif;
  }
  
  /* ======= BOTÓN ======= */
  .gradient-border-button {
    position: relative;
    width: 252px;
    height: 55px;
    padding: 17px 20px;
    background: #092514A6;
    border: none;
    border-radius: 5px;
    color: white;
    font-weight: 500;
    font-size: 16px;
    cursor: pointer;
    transition: all 0.3s ease;
    z-index: 10;
    text-align: center;
  }
  
  .gradient-border-button::before {
    content: '';
    position: absolute;
    inset: 0;
    border-radius: 5px;
    padding: 1px;
    background: linear-gradient(237.56deg, #2DFF45 1.39%, #9BFF9B 47.74%, #2DFF3B 100%);
    -webkit-mask: 
      linear-gradient(#fff 0 0) content-box, 
      linear-gradient(#fff 0 0);
    -webkit-mask-composite: xor;
    mask-composite: exclude;
    z-index: -1;
  }
  
  .gradient-border-button:hover {
    background: #0a2916;
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(45, 255, 69, 0.3);
  }

  .title {
    font-family: 'Avory I PE', sans-serif;
    font-size: 50px;
    font-weight: 800;
    letter-spacing: 2%;
    line-height: 45px;
    color: white;
    text-transform: uppercase;
    position: relative;
    z-index: 10;
  }
  
  /* Responsive tablet */
  @media (max-width: 1024px) {
    .gradient-border-button {
      margin: 20px 20px 0 20px;
      width: 100%;
      max-width: 90%;
      height: 66px;
    }

    .gradient-border-button {
     font-size: 20px;
    }

    .player-card {
      width: 450px;
    }
  }

  /* Responsive mobile */
  @media (max-width: 767px) {
    .player-card {
      width: 100%;
      padding: 30px 20px;
      min-height: auto;
    }

    .gradient-border-button {
      margin: 0 20px;
      width: 100%;
      max-width: 90%;
      height: 66px;
    }
    
    .card-title {
      font-size: 20px;
      margin-bottom: 20px;
    }

    .title {
      font-size: 35px;
    }
    
    .card-user {
      font-size: 20px;
    }
    
    .card-comment {
      font-size: 20px;
    }
  }
</style>

<script>
  import Swiper from 'swiper';
  import { Autoplay } from 'swiper/modules';
  import 'swiper/css';

  const selector = '.players-swiper';
  const swiperMap = new WeakMap();

  function initPlayersCarousel() {
    const el = document.querySelector(selector);
    if (!el) return;
    const slidesCount = el.querySelectorAll('.swiper-slide').length;
    if (slidesCount <= 1) return;
    if (el.classList.contains('swiper-initialized')) return;
    if (swiperMap.has(el)) { try { const s = swiperMap.get(el); s && s.update && s.update(); } catch {} return; }
    const s = new Swiper(selector, {
      modules: [Autoplay],
      slidesPerView: 2.8,
      spaceBetween: 25,
      centeredSlides: false,
      loop: true,
      autoplay: { delay: 2000, disableOnInteraction: false, pauseOnMouseEnter: false },
      speed: 800,
      breakpoints: { 320: { slidesPerView: 1, spaceBetween: 20 }, 768: { slidesPerView: 1.8, spaceBetween: 30 }, 1024: { slidesPerView: 2, spaceBetween: 30 }, 1440: { slidesPerView: 2.5, spaceBetween: 30 } }
    });
    swiperMap.set(el, s);
  }

  function destroyPlayersCarousel() {
    const el = document.querySelector(selector);
    if (!el) return;
    const s = swiperMap.get(el);
    if (s && s.destroy) { try { s.destroy(true, true); } catch {} }
    swiperMap.delete(el);
  }

  if (document.readyState === 'loading') { document.addEventListener('DOMContentLoaded', initPlayersCarousel); } else { initPlayersCarousel(); }
  document.addEventListener('astro:page-load', initPlayersCarousel);
  document.addEventListener('astro:after-swap', initPlayersCarousel);
  document.addEventListener('astro:before-swap', destroyPlayersCarousel);
</script>